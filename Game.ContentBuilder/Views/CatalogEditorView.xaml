<UserControl x:Class="Game.ContentBuilder.Views.CatalogEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Game.ContentBuilder.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Collapsible Metadata Header -->
        <Expander Grid.Row="0" 
                  IsExpanded="False"
                  AutomationProperties.AutomationId="MetadataExpander"
                  materialDesign:ExpanderAssist.HorizontalHeaderPadding="16,12"
                  Background="{DynamicResource MaterialDesignPaper}"
                  Margin="0,0,0,8">
            <Expander.Header>
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="InformationOutline" Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="METADATA" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center"/>
                </StackPanel>
            </Expander.Header>
            <Grid Margin="16" AutomationProperties.AutomationId="MetadataPanel">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBox Grid.Row="0" Grid.Column="0"
                         Margin="0,0,8,12"
                         FontSize="12"
                         materialDesign:HintAssist.Hint="Description"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         AutomationProperties.AutomationId="CatalogDescriptionTextBox"
                         Text="{Binding MetadataDescription, UpdateSourceTrigger=PropertyChanged}"/>
                
                <TextBox Grid.Row="0" Grid.Column="1"
                         Margin="8,0,0,12"
                         FontSize="12"
                         materialDesign:HintAssist.Hint="Type"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Text="{Binding MetadataType, UpdateSourceTrigger=PropertyChanged}"/>
                
                <TextBox Grid.Row="1" Grid.Column="0"
                         Margin="0,0,8,12"
                         FontSize="12"
                         materialDesign:HintAssist.Hint="Version"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         AutomationProperties.AutomationId="CatalogVersionTextBox"
                         Text="{Binding MetadataVersion, UpdateSourceTrigger=PropertyChanged}"/>
                
                <TextBox Grid.Row="1" Grid.Column="1"
                         Margin="8,0,0,12"
                         FontSize="12"
                         materialDesign:HintAssist.Hint="Usage"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Text="{Binding MetadataUsage, UpdateSourceTrigger=PropertyChanged}"/>
                
                <TextBox Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                         Margin="0,0,0,0"
                         FontSize="12"
                         materialDesign:HintAssist.Hint="Notes"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         AcceptsReturn="True"
                         Height="80"
                         VerticalScrollBarVisibility="Auto"
                         Text="{Binding MetadataNotesText, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
        </Expander>

        <!-- Two Column Layout: Catalogs (Left) and Details (Right) -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Catalogs (Left) with TreeView -->
            <materialDesign:Card Grid.Column="0" Margin="0,0,4,0" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <Border Grid.Row="0" Background="{DynamicResource MaterialDesignPaper}" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="16,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ShapeOutline" Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="TYPE CATALOGS" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding TypeCatalogs.Count, StringFormat=' ({0} types)'}" FontSize="12" Foreground="Gray" Margin="4,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <TreeView x:Name="CatalogTreeView"
                                  AutomationProperties.AutomationId="CatalogTreeView"
                                  AutomationProperties.Name="Category Tree View"
                                  Margin="16,8,16,8" 
                                  ItemsSource="{Binding TypeCatalogs}" 
                                  BorderThickness="0"
                                  SelectedItemChanged="CatalogTreeView_SelectedItemChanged">
                            <TreeView.ItemTemplate>
                                <HierarchicalDataTemplate ItemsSource="{Binding Categories}">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="ShapeOutline" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                    </StackPanel>
                                    <HierarchicalDataTemplate.ItemTemplate>
                                        <HierarchicalDataTemplate ItemsSource="{Binding Items}">
                                            <StackPanel Orientation="Horizontal">
                                                <materialDesign:PackIcon Kind="FolderOutline" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                                                         BorderThickness="0" Background="Transparent"
                                                         MinWidth="100"
                                                         AutomationProperties.AutomationId="CategoryNameTextBox"/>
                                            </StackPanel>
                                            <HierarchicalDataTemplate.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <materialDesign:PackIcon Kind="ShapeSquarePlus" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                                        <TextBlock Text="{Binding Name}"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </HierarchicalDataTemplate.ItemTemplate>
                                        </HierarchicalDataTemplate>
                                    </HierarchicalDataTemplate.ItemTemplate>
                                </HierarchicalDataTemplate>
                            </TreeView.ItemTemplate>
                        </TreeView>
                    </ScrollViewer>
                    
                    <StackPanel Grid.Row="2" Margin="16,12,16,16">                        
                        <Button Command="{Binding AddCategoryCommand}" 
                                AutomationProperties.AutomationId="AddCategoryButton"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Margin="0,0,0,4">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                <TextBlock Text="Add Category"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding AddItemCommand}" 
                                AutomationProperties.AutomationId="AddItemButton"
                                Style="{StaticResource MaterialDesignOutlinedButton}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                <TextBlock Text="Add Item"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>

            <!-- Item Details (Right) -->
            <materialDesign:Card Grid.Column="1" Margin="4,0,0,0" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{DynamicResource MaterialDesignPaper}" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="16,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FileDocumentOutline" Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="ITEM DETAILS" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="12">
                        <StackPanel>
                            <!-- Category Traits Section -->
                            <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="0,0,0,16" Margin="0,0,0,16"
                                    Visibility="{Binding ShowCategoryTraits, Converter={StaticResource BooleanToVisibilityConverter}}"
                                    AutomationProperties.AutomationId="CategoryTraitsPanel">
                                <StackPanel DataContext="{Binding SelectedCategory}">
                                    <TextBlock Text="Category Traits" 
                                               Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                               Margin="0,0,0,8"/>
                                    
                                    <TextBlock Text="{Binding Name, StringFormat='Category: {0}'}"
                                               FontWeight="SemiBold"
                                               Margin="0,0,0,12"/>

                                    <ItemsControl ItemsSource="{Binding Traits}"
                                                  AutomationProperties.AutomationId="CategoryTraitsList">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,0,0,8">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBox Grid.Column="0"
                                                             Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}"
                                                             materialDesign:HintAssist.Hint="Trait Name"
                                                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                                             Margin="0,0,4,0"/>
                                                    <TextBox Grid.Column="1"
                                                             Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                                             materialDesign:HintAssist.Hint="Trait Value"
                                                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                                             Margin="4,0,4,0"/>
                                                    <Button Grid.Column="2"
                                                            Command="{Binding DataContext.RemoveTraitCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding Key}"
                                                            Style="{StaticResource MaterialDesignIconButton}"
                                                            ToolTip="Remove trait"
                                                            AutomationProperties.AutomationId="DeleteTraitButton">
                                                        <materialDesign:PackIcon Kind="Close" Width="16" Height="16"/>
                                                    </Button>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <Button Command="{Binding DataContext.AddTraitCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            Style="{StaticResource MaterialDesignOutlinedButton}"
                                            AutomationProperties.AutomationId="AddTraitButton"
                                            Margin="0,8,0,0">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                            <TextBlock Text="Add Trait"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Border>

                            <!-- Item Details Section -->
                            <StackPanel DataContext="{Binding SelectedItem}"
                                        Visibility="{Binding Converter={StaticResource NotNullToVisibilityConverter}}"
                                        AutomationProperties.AutomationId="ItemDetailsPanel">
                            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                     materialDesign:HintAssist.Hint="Item Name"
                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                     AutomationProperties.AutomationId="ItemNameTextBox"
                                     Margin="0,0,0,16"/>

                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBox Grid.Column="0"
                                         Text="{Binding RarityWeight, UpdateSourceTrigger=PropertyChanged}"
                                         materialDesign:HintAssist.Hint="Rarity Weight"
                                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                         AutomationProperties.AutomationId="ItemRarityWeightTextBox"
                                         ToolTip="Numeric weight for rarity calculation"/>
                                
                                <Border Grid.Column="1"
                                        Background="{Binding RarityWeight, Converter={StaticResource RarityWeightToColorConverter}}"
                                        CornerRadius="12"
                                        Padding="12,4"
                                        Margin="8,0,0,0"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="{Binding RarityWeight, Converter={StaticResource RarityWeightToNameConverter}}"
                                               FontSize="11"
                                               FontWeight="Bold"
                                               Foreground="White"/>
                                </Border>
                            </Grid>

                            <TextBlock Text="Properties" 
                                       Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                       Margin="0,0,0,8"/>

                            <ItemsControl ItemsSource="{Binding Properties}"
                                          AutomationProperties.AutomationId="ItemPropertiesPanel">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBox Grid.Column="0"
                                                     Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}"
                                                     materialDesign:HintAssist.Hint="Key"
                                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                                     AutomationProperties.AutomationId="CustomFieldKeyTextBox"
                                                     Margin="0,0,4,0"/>
                                            <TextBox Grid.Column="1"
                                                     Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                                     materialDesign:HintAssist.Hint="Value"
                                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                                     AutomationProperties.AutomationId="CustomFieldValueTextBox"
                                                     Margin="4,0,0,0"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <Button Command="{Binding DataContext.AddPropertyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    AutomationProperties.AutomationId="AddCustomFieldButton"
                                    Margin="0,8,0,16">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBlock Text="Add Property"/>
                                </StackPanel>
                            </Button>

                            <TextBlock Text="Traits" 
                                       Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                       Margin="0,0,0,8"/>

                            <ItemsControl ItemsSource="{Binding Traits}"
                                          AutomationProperties.AutomationId="ItemTraitsPanel">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" Margin="0,0,0,4"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </materialDesign:Card>
        </Grid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2"
                   Background="{DynamicResource MaterialDesignPaper}"
                   BorderBrush="#E0E0E0"
                   BorderThickness="0,1,0,0"
                   Padding="16,8"
                   AutomationProperties.AutomationId="StatusBar">
            <StatusBarItem HorizontalContentAlignment="Stretch">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0"
                               VerticalAlignment="Center"
                               FontSize="12"
                               Text="{Binding StatusMessage}"/>
                    
                    <Button Grid.Column="1"
                            Command="{Binding SaveCommand}"
                            AutomationProperties.AutomationId="SaveButton"
                            Content="SAVE CHANGES"
                            Style="{StaticResource MaterialDesignRaisedButton}"/>
                </Grid>
            </StatusBarItem>
        </StatusBar>


    </Grid>
</UserControl>
