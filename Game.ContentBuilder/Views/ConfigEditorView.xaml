<UserControl x:Class="Game.ContentBuilder.Views.ConfigEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="{Binding FileName}" 
                       Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                       Margin="0,0,0,8"/>
            <TextBlock Text="Folder Configuration Editor (.cbconfig.json)" 
                       Style="{StaticResource MaterialDesignCaptionTextBlock}"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
        </StackPanel>

        <!-- Configuration Form -->
        <GroupBox Grid.Row="1" 
                  Header="Folder Settings"
                  Style="{StaticResource MaterialDesignGroupBox}"
                  Margin="0,0,0,16">
            <StackPanel Margin="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Display Name -->
                    <TextBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"
                             Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Display Name *"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

                    <!-- Icon -->
                    <TextBox Grid.Row="2" Grid.Column="0"
                             Text="{Binding Icon, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="MaterialDesign Icon Name *"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

                    <!-- Icon Preview -->
                    <Border Grid.Row="2" Grid.Column="2"
                            Width="56" Height="56"
                            Background="{DynamicResource MaterialDesignChipBackground}"
                            CornerRadius="4">
                        <materialDesign:PackIcon Kind="{Binding Icon}"
                                                 Width="32" Height="32"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"/>
                    </Border>

                    <!-- Sort Order -->
                    <TextBox Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3"
                             Text="{Binding SortOrder, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Sort Order *"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

                    <!-- Description (Optional) -->
                    <TextBox Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="3"
                             Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Description (optional)"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             MinLines="3"/>

                    <!-- Default File Icon (Optional) -->
                    <TextBox Grid.Row="8" Grid.Column="0"
                             Text="{Binding DefaultFileIcon, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Default File Icon (optional)"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

                    <!-- Default File Icon Preview -->
                    <Border Grid.Row="8" Grid.Column="2"
                            Width="56" Height="56"
                            Background="{DynamicResource MaterialDesignChipBackground}"
                            CornerRadius="4"
                            Visibility="{Binding DefaultFileIcon, Converter={StaticResource NullToVisibilityConverter}}">
                        <materialDesign:PackIcon Kind="{Binding DefaultFileIcon}"
                                                 Width="24" Height="24"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"/>
                    </Border>
                </Grid>

                <TextBlock Text="* Required fields" 
                           Style="{StaticResource MaterialDesignCaptionTextBlock}"
                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                           Margin="0,16,0,0"/>
            </StackPanel>
        </GroupBox>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,0,0,16">
            <Button Command="{Binding SaveCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="0,0,8,0"
                    IsEnabled="{Binding IsDirty}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ContentSave" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="Save"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding RefreshCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Refresh" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="Refresh"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- Raw JSON Preview -->
        <GroupBox Grid.Row="3" 
                  Header="JSON Preview"
                  Style="{StaticResource MaterialDesignGroupBox}">
            <TextBox Text="{Binding RawJsonPreview, Mode=OneWay}"
                     IsReadOnly="True"
                     FontFamily="Consolas"
                     FontSize="11"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Background="{DynamicResource MaterialDesignTextFieldBoxBackground}"/>
        </GroupBox>
    </Grid>
</UserControl>
