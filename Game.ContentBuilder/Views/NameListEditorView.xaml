<UserControl x:Class="Game.ContentBuilder.Views.NameListEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Main Panel: Pattern Tokens, Component Keys, Components -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,8,0">
                <StackPanel>
                    <!-- Pattern Tokens -->
                    <TextBlock Text="Pattern Tokens" FontWeight="Bold" Margin="0,8,0,4"/>
                    <ItemsControl ItemsSource="{Binding PatternTokens}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBox Text="{Binding .}" Width="120" Margin="0,0,8,0"/>
                                    <!-- Add remove button if needed -->
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <!-- TODO: Add Add/Remove PatternToken buttons -->

                    <!-- Component Keys -->
                    <TextBlock Text="Component Keys" FontWeight="Bold" Margin="0,16,0,4"/>
                    <ItemsControl ItemsSource="{Binding ComponentKeys}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBox Text="{Binding .}" Width="120" Margin="0,0,8,0"/>
                                    <!-- Add remove button if needed -->
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <!-- TODO: Add Add/Remove ComponentKey buttons -->

                    <!-- Dynamic Components -->
                    <ItemsControl ItemsSource="{Binding ComponentNames}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding .}" FontWeight="Bold" Margin="0,16,0,4"/>
                                    <ItemsControl ItemsSource="{Binding DataContext.Components[.], RelativeSource={RelativeSource AncestorType=UserControl}}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                                    <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="120" Margin="0,0,8,0"/>
                                                    <TextBox Text="{Binding RarityWeight, UpdateSourceTrigger=PropertyChanged}" Width="60" Margin="0,0,8,0"/>
                                                    <TextBox Text="{Binding Gender, UpdateSourceTrigger=PropertyChanged}" Width="60" Margin="0,0,8,0"/>
                                                    <TextBox Text="{Binding PreferredSocialClass, UpdateSourceTrigger=PropertyChanged}" Width="80" Margin="0,0,8,0"/>
                                                    <!-- TODO: Add WeightMultiplier editor -->
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Save/Cancel Buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                        <Button Content="CANCEL" Command="{Binding CancelCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,8,0"/>
                        <Button Content="SAVE" Command="{Binding SaveCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>

            <!-- Metadata Panel -->
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <materialDesign:Card Padding="16">
                    <StackPanel>
                        <TextBlock Text="Metadata &amp; Notes" FontSize="16" FontWeight="Bold" Margin="0,0,0,16"/>
                        <TextBlock Text="Description" Style="{StaticResource MaterialDesignCaptionTextBlock}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding Metadata.Description, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Brief description of this file" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,16"/>
                        <TextBlock Text="Version" Style="{StaticResource MaterialDesignCaptionTextBlock}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding Metadata.Version, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="e.g., 1.0, 2.3.1" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,16"/>
                        <TextBlock Text="Rarity System" Style="{StaticResource MaterialDesignCaptionTextBlock}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding Metadata.RaritySystem, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="e.g., weight-based" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,16"/>
                        <TextBlock Text="Supports Soft Filtering" Style="{StaticResource MaterialDesignCaptionTextBlock}" Margin="0,0,0,4"/>
                        <CheckBox IsChecked="{Binding Metadata.SupportsSoftFiltering, UpdateSourceTrigger=PropertyChanged}" Content="Yes" Margin="0,0,0,16"/>
                        <TextBlock Text="Notes" Style="{StaticResource MaterialDesignCaptionTextBlock}" Margin="0,0,0,4"/>
                        <ItemsControl ItemsSource="{Binding Metadata.Notes}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding .}" Margin="0,0,0,4"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <!-- TODO: Add Add/Remove Note buttons -->
                        <TextBlock Text="Last Modified" Style="{StaticResource MaterialDesignCaptionTextBlock}" Margin="0,16,0,4"/>
                        <TextBox Text="{Binding Metadata.LastModified}" IsReadOnly="True" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </StackPanel>
                </materialDesign:Card>
            </ScrollViewer>
        </Grid>

        <!-- Status Bar -->
        <materialDesign:ColorZone Grid.Row="1" Mode="PrimaryLight" Padding="8">
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center"/>
        </materialDesign:ColorZone>
    </Grid>
</UserControl>
