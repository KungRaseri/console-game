<UserControl x:Class="Game.ContentBuilder.Views.NamesEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Game.ContentBuilder.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:Card Grid.Row="0" Margin="8">
            <StackPanel Margin="16">
                <TextBlock Text="Names Editor (Pattern Generation)" 
                           Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                <TextBlock Text="{Binding FileName}" 
                           Style="{StaticResource MaterialDesignBody2TextBlock}"
                           Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- Metadata Section -->
        <Expander Grid.Row="1" Margin="8" Header="Metadata" IsExpanded="False">
            <StackPanel Margin="16,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Version:" Margin="0,0,8,4" FontWeight="Bold"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding MetadataVersion}" Margin="0,0,0,4"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Type:" Margin="0,0,8,4" FontWeight="Bold"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding MetadataType}" Margin="0,0,0,4"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Description:" Margin="0,0,8,4" FontWeight="Bold"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding MetadataDescription}" TextWrapping="Wrap" Margin="0,0,0,4"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Usage:" Margin="0,0,8,4" FontWeight="Bold"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding MetadataUsage}" TextWrapping="Wrap" Margin="0,0,0,4"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Notes:" Margin="0,0,8,4" FontWeight="Bold" VerticalAlignment="Top"/>
                    <ItemsControl Grid.Row="4" Grid.Column="1" ItemsSource="{Binding MetadataNotes}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,0,0,2"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </StackPanel>
        </Expander>

        <!-- Main Content -->
        <Grid Grid.Row="2" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Components Section -->
            <materialDesign:Card Grid.Column="0" Margin="0,0,4,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Components Header -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="16,16,16,8">
                        <TextBlock Text="Components" Style="{StaticResource MaterialDesignHeadline6TextBlock}" VerticalAlignment="Center"/>
                        <Button Command="{Binding AddComponentGroupCommand}" 
                                Style="{StaticResource MaterialDesignIconButton}"
                                ToolTip="Add Component Group"
                                Margin="8,0,0,0">
                            <materialDesign:PackIcon Kind="Plus"/>
                        </Button>
                        <Button Command="{Binding RemoveComponentGroupCommand}" 
                                Style="{StaticResource MaterialDesignIconButton}"
                                ToolTip="Remove Component Group">
                            <materialDesign:PackIcon Kind="Minus"/>
                        </Button>
                    </StackPanel>

                    <!-- Components Tree -->
                    <Grid Grid.Row="1" Margin="16,0,16,16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TreeView Grid.Row="0" ItemsSource="{Binding ComponentGroups}">
                            <TreeView.ItemTemplate>
                                <HierarchicalDataTemplate ItemsSource="{Binding Components}">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="FolderOutline" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                                                 BorderThickness="0" Background="Transparent"
                                                 MinWidth="100"/>
                                    </StackPanel>
                                    <HierarchicalDataTemplate.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <materialDesign:PackIcon Kind="TextBox" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                                <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" 
                                                         BorderThickness="0" Background="Transparent"
                                                         MinWidth="150"/>
                                                <TextBlock Text=" (Weight: " Margin="4,0,0,0"/>
                                                <TextBox Text="{Binding Weight, UpdateSourceTrigger=PropertyChanged}" 
                                                         BorderThickness="0" Background="Transparent"
                                                         Width="30"/>
                                                <TextBlock Text=")"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </HierarchicalDataTemplate.ItemTemplate>
                                </HierarchicalDataTemplate>
                            </TreeView.ItemTemplate>
                        </TreeView>

                        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                            <Button Command="{Binding AddComponentCommand}" 
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    ToolTip="Add Component to Selected Group"
                                    Margin="0,0,8,0">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBlock Text="Add Component"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding RemoveComponentCommand}" 
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    ToolTip="Remove Selected Component">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Minus" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBlock Text="Remove"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Grid>
            </materialDesign:Card>

            <!-- Patterns Section -->
            <materialDesign:Card Grid.Column="1" Margin="4,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Patterns Header -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="16,16,16,8">
                        <TextBlock Text="Patterns" Style="{StaticResource MaterialDesignHeadline6TextBlock}" VerticalAlignment="Center"/>
                        <Button Command="{Binding AddPatternCommand}" 
                                Style="{StaticResource MaterialDesignIconButton}"
                                ToolTip="Add Pattern"
                                Margin="8,0,0,0">
                            <materialDesign:PackIcon Kind="Plus"/>
                        </Button>
                        <Button Command="{Binding RemovePatternCommand}" 
                                Style="{StaticResource MaterialDesignIconButton}"
                                ToolTip="Remove Pattern">
                            <materialDesign:PackIcon Kind="Minus"/>
                        </Button>
                    </StackPanel>

                    <!-- Patterns List -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="16,0,16,16">
                        <ItemsControl ItemsSource="{Binding Patterns}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <materialDesign:Card Margin="0,0,0,8">
                                        <StackPanel Margin="12">
                                            <TextBox Text="{Binding Pattern, UpdateSourceTrigger=PropertyChanged}"
                                                     materialDesign:HintAssist.Hint="Pattern"
                                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                                     Margin="0,0,0,8"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBox Grid.Column="0"
                                                         Text="{Binding Weight, UpdateSourceTrigger=PropertyChanged}"
                                                         materialDesign:HintAssist.Hint="Weight"
                                                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                                         Width="80"
                                                         Margin="0,0,8,8"/>
                                                <TextBox Grid.Column="1"
                                                         Text="{Binding Example, UpdateSourceTrigger=PropertyChanged}"
                                                         materialDesign:HintAssist.Hint="Example"
                                                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                                         Margin="0,0,0,8"/>
                                            </Grid>
                                        </StackPanel>
                                    </materialDesign:Card>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </materialDesign:Card>
        </Grid>

        <!-- Footer -->
        <materialDesign:Card Grid.Row="3" Margin="8">
            <Grid Margin="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" VerticalAlignment="Center"/>
                
                <Button Grid.Column="1" 
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        IsEnabled="{Binding IsDirty}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ContentSave" VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <TextBlock Text="Save"/>
                    </StackPanel>
                </Button>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
